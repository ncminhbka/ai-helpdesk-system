services:
  # ==================== PostgreSQL + pgvector ====================
  db:
    image: pgvector/pgvector:pg17
    container_name: fpt_helpdesk_db
    restart: unless-stopped
    env_file:
      - ./backend/.env
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME:-fpt_helpdesk}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-fpt_helpdesk}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ==================== FastAPI Backend ====================
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: fpt_helpdesk_backend
    restart: unless-stopped
    env_file:
      - ./backend/.env
    environment:
      # Override DB_HOST to point to the db service (not localhost)
      DB_HOST: db
    ports:
      - "8000:8000"
    volumes:
      # Mount docs for ingest.py (if needed inside container)
      - ./docs:/app/docs:ro
    depends_on:
      db:
        condition: service_healthy

  # ==================== React Frontend ====================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: fpt_helpdesk_frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  postgres_data:
    driver: local
